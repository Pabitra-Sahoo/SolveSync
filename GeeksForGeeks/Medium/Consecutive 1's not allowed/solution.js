/**\n * @param {number} n\n * @returns {number}\n */\nclass Solution {\n    countStrings(n) {\n        // Base case: for n = 1, strings are "0" and "1"\n        if (n === 1) return 2;\n\n        // a[i] = count of strings of length i ending in 0\n        // b[i] = count of strings of length i ending in 1\n        let a = 1; \n        let b = 1;\n\n        for (let i = 2; i <= n; i++) {\n            let temp = a;\n            a = a + b; // New strings ending in 0\n            b = temp;  // New strings ending in 1 (must follow a 0)\n        }\n\n        return a + b;\n    }\n}