                \n                // Spread to parent\n                if (parentMap.containsKey(current) && !visited.contains(parentMap.get(current))) {\n                    visited.add(parentMap.get(current));\n                    queue.add(parentMap.get(current));\n                }\n            }\n            maxTime++;\n        }\n        \n        return maxTime - 1; // subtract 1 as we count one extra iteration\n    }\n    \n    private Node findTarget(Node root, int target, Map<Node, Node> parentMap) {\n        if (root == null) return null;\n        \n        Queue<Node> queue = new LinkedList<>();\n        queue.add(root);\n        \n        while (!queue.isEmpty()) {\n            Node current = queue.poll();\n            \n            if (current.data == target) return current;\n            \n            if (current.left != null) {\n                parentMap.put(current.left, current);\n                queue.add(current.left);\n            }\n            \n            if (current.right != null) {\n                parentMap.put(current.right, current);\n                queue.add(current.right);\n            }\n        }\n        \n        return null;\n    }\n}