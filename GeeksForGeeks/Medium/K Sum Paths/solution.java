class Solution {\n    public int countAllPaths(Node root, int k) {\n        HashMap<Integer, Integer> map = new HashMap<>();\n        map.put(0, 1); // Base case: sum 0 with frequency 1\n        return dfs(root, k, 0, map);\n    }\n    \n    private int dfs(Node node, int k, int currentSum, HashMap<Integer, Integer> map) {\n        if (node == null) {\n            return 0;\n        }\n        \n        // Update current sum\n        currentSum += node.data;\n        \n        // Count paths where (currentSum - k) exists\n        int count = map.getOrDefault(currentSum - k, 0);\n        \n        // Add current sum to map\n        map.put(currentSum, map.getOrDefault(currentSum, 0) + 1);\n        \n        // Explore left and right subtrees\n        count += dfs(node.left, k, currentSum, map);\n        count += dfs(node.right, k, currentSum, map);\n        \n        // Backtrack: remove current sum\n        map.put(currentSum, map.get(currentSum) - 1);\n        if (map.get(currentSum) == 0) {\n            map.remove(currentSum);\n        }\n        \n        return count;\n    }\n}