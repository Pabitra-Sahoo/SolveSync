class Solution {\n    public static boolean checkEqual(int[] a, int[] b) {\n        // If sizes are different, arrays can't be equal\n        if (a.length != b.length) {\n            return false;\n        }\n        \n        // Use HashMap to store frequency of elements from array a\n        java.util.HashMap<Integer, Integer> map = new java.util.HashMap<>();\n        \n        // Add all elements from array a to map with their frequencies\n        for (int num : a) {\n            map.put(num, map.getOrDefault(num, 0) + 1);\n        }\n        \n        // Check if all elements in array b exist in map with same frequency\n        for (int num : b) {\n            if (!map.containsKey(num)) {\n                return false;\n            }\n            map.put(num, map.get(num) - 1);\n            if (map.get(num) < 0) {\n                return false;\n            }\n        }\n        \n        return true;\n    }\n}